# CLAUDE.md — 이 저장소에서 클로드 코드가 지켜야 할 규칙

> 이 파일은 클로드 코드가 **자동으로 읽는** 파일입니다.
> 규민·재환·위진 세 명 모두 같은 규칙 아래에서 작업하도록 만드는 장치입니다.
> 사람이 읽어도 되는 팀 규칙서이기도 합니다.

## 0. 팀 구성

| 이름 | GitHub 역할 | 할 수 있는 일 |
|---|---|---|
| 규민 | Admin / 유일한 리뷰어 | 리뷰, 머지, `main` 관리, 배포, DB 스키마 변경 |
| 재환 | Write | 브랜치 생성, 커밋, 푸시, PR 생성 (머지는 불가) |
| 위진 | Write | 브랜치 생성, 커밋, 푸시, PR 생성 (머지는 불가) |

**재환과 위진은 개발이 처음입니다.** 설명은 항상 한국어로, 전문용어는 처음 나올 때 괄호로 풀어서 씁니다.

## 1. 클로드가 항상 지킬 것

### 1-1. 절대 하지 말 것
- `main` 브랜치에 직접 커밋하거나 푸시하지 않는다. **항상 새 브랜치를 만든다.**
- `git push --force`, `git reset --hard`, `git clean -fd` 를 사용자 요청 없이 실행하지 않는다.
- PR을 **머지하지 않는다**. 머지는 규민만 GitHub 웹에서 한다.
- `.env.local` 을 만들거나 수정하는 건 되지만, 그 안의 값을 절대 커밋하거나 화면에 출력하지 않는다.
- 사용자가 요청하지 않은 파일을 새로 만들지 않는다 (특히 README, 요약 문서).

### 1-2. 항상 할 것
- 작업 시작 전에 `git status` 와 `git branch --show-current` 를 확인하고, `main` 위에 있으면 **먼저 브랜치를 만든다.**
- 커밋 메시지는 아래 3번의 형식을 따른다.
- 코드를 고친 뒤에는 `npm run typecheck` 와 `npm run lint` 를 돌려 통과를 확인한다.
- 명령어를 알려줄 때는 **왜 그 명령어가 필요한지 한 줄 설명을 붙인다.**

### 1-3. 재환·위진과 대화할 때
- 에러 메시지가 뜨면, 원문을 그대로 해석해주고 **다음에 칠 명령어 하나만** 알려준다. 여러 개를 한꺼번에 주지 않는다.
- "브랜치", "머지", "커밋" 같은 단어는 [docs/01-git-기초.md](docs/01-git-기초.md) 의 비유(작업 사본 / 합치기 / 저장 지점)를 그대로 사용한다.
- 작업이 끝나면 **PR 링크를 만들어 주고, 카톡방에 붙여넣을 문장까지 만들어 준다.**

## 2. 브랜치 규칙

```
main                    ← 배포되는 진짜 코드. 규민만 건드림.
 ├─ feat/기능이름         ← 새 기능
 ├─ fix/버그이름          ← 버그 수정
 ├─ docs/문서이름         ← 문서만 수정
 └─ chore/작업이름        ← 설정, 정리
```

브랜치 이름은 영어 소문자와 하이픈만 사용합니다. 예: `feat/comment-form`, `fix/vote-count`.

## 3. 커밋 메시지 형식

```
<종류>: <무엇을 했는지 한글 한 줄>

(선택) 왜 그렇게 했는지 설명
```

종류는 `feat`(기능 추가), `fix`(버그 수정), `docs`(문서), `style`(디자인/서식), `refactor`(동작 그대로 정리), `chore`(설정) 중 하나.

예시:
```
feat: 아이디어에 댓글 남기는 폼 추가
fix: 투표 취소했을 때 숫자가 안 줄어드는 문제 수정
docs: 재환/위진용 첫 설치 가이드 추가
```

## 4. 코드 스타일

- 언어: TypeScript. `any` 를 쓰지 않는다.
- 컴포넌트는 기본이 **서버 컴포넌트**. 클릭·입력 등 상호작용이 필요할 때만 파일 맨 위에 `"use client"` 를 붙인다.
- 데이터 검증은 반드시 `lib/validation.ts` 의 zod 스키마를 거친다. API에서도 다시 검증한다.
- API 응답은 항상 `lib/api.ts` 의 `ok()` / `fail()` 을 쓴다. 형태를 통일하기 위해서다.
- 주석은 "무엇을"이 아니라 **"왜"** 를 적는다.

## 5. 자주 쓰는 명령어

```bash
npm run dev        # 개발 서버 실행 → http://localhost:3000
npm run typecheck  # 타입 오류 검사 (PR 전에 필수)
npm run lint       # 코드 스타일 검사 (PR 전에 필수)
npm run build      # 실제 배포와 같은 방식으로 빌드
```

## 6. 폴더 구조

```
app/          화면과 API (Next.js App Router)
  page.tsx           홈 = 아이디어 목록
  ideas/new/         아이디어 작성 화면
  ideas/[id]/        아이디어 상세 화면
  api/               서버 API
components/   화면 조각 (버튼, 폼 등)
lib/          공용 로직 (DB 연결, 검증, 응답 형식)
db/schema.sql 데이터베이스 설계도
docs/         팀 문서 (초보자 가이드, 회의 규칙)
```

## 7. 데이터베이스를 바꿔야 할 때

`db/schema.sql` 은 **규민의 승인 없이 수정하지 않는다.** 스키마 변경이 필요하면
PR 설명에 "DB 변경 필요"라고 적고 규민에게 알린다. 스키마는 한 번 꼬이면
세 명 모두의 로컬 환경이 동시에 망가지기 때문이다.
